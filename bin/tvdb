#!/usr/bin/env python

import sys
import os

if __name__ == "__main__":
    root = os.path.dirname(os.path.dirname(__file__))

    if os.path.isfile(os.path.join(root, "tvdb", "__init__.py")):
        sys.path.insert(0, root)

    from tvdb import tvdb_main
    sys.exit(tvdb_main(sys.argv[1:]))
